export enum Path {
  home = '/',
  login = 'login',
  register = 'register',
  unauthorized = '/unauthorized',
}

export enum ApiEndpoints {
  register = 'auth/register',
  login = 'auth/login',
  refresh = 'auth/refresh',
  logout = 'auth/logout',
  verify = 'auth',
}
